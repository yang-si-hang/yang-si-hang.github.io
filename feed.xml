<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yang-si-hang.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yang-si-hang.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-21T07:03:01+00:00</updated><id>https://yang-si-hang.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Implementing Knowledge Insulation in OpenPI</title><link href="https://yang-si-hang.github.io/blog/2026/openpi-knowledge-insulation/" rel="alternate" type="text/html" title="Implementing Knowledge Insulation in OpenPI"/><published>2026-09-20T08:32:00+00:00</published><updated>2026-09-20T08:32:00+00:00</updated><id>https://yang-si-hang.github.io/blog/2026/openpi-knowledge-insulation</id><content type="html" xml:base="https://yang-si-hang.github.io/blog/2026/openpi-knowledge-insulation/"><![CDATA[<h1 id="why-sharing-the-vlm-forward-can-change-the-objective">Why Sharing the VLM Forward Can Change the Objective</h1> <p>Knowledge Insulation (KI) is a training strategy introduced by Physical Intelligence to combine the semantic capabilities of a pretrained vision-language model (VLM) with a continuous action expert, while preventing continuous control optimization from directly modifying the VLM representation.</p> <p>The basic idea is simple: a discrete autoregressive action objective trains the VLM, while the continuous flow-matching objective trains the action expert with gradients blocked from flowing back into the VLM.</p> <p>Physical Intelligence describes the method in:</p> <p><a href="https://www.pi.website/research/knowledge_insulation">VLAs that Train Fast, Run Fast, and Generalize Better</a></p> <p>I implemented and experimented with KI for π0.5 in my OpenPI fork:</p> <p><a href="https://github.com/yang-si-hang/openpi05-post-train">openpi05-post-train</a></p> <p>This post focuses on one implementation detail that turned out to be important:</p> <blockquote> <p>Sharing VLM <strong>parameters</strong> between the FAST and flow-matching branches is natural. Sharing the same VLM <strong>forward pass or KV cache</strong> is an additional assumption, and may require changing the training objective.</p> </blockquote> <p>In the implementation studied here, forcing the two branches to share a single VLM prefix changed the FAST autoregressive sequence itself. Experiments show that this change significantly alters the VLM optimization direction. However, the results do <strong>not</strong> establish the exact serialization used during π0.5 pretraining.</p> <h2 id="1-knowledge-insulation-in-the-openpi-implementation">1. Knowledge Insulation in the OpenPI implementation</h2> <p>The implementation optimizes two objectives:</p> \[L = \lambda_{\mathrm{FAST}}L_{\mathrm{FAST}} + \lambda_{\mathrm{FM}}L_{\mathrm{FM}}.\] <p>The FAST branch predicts discretized action tokens autoregressively. Its gradients update the vision encoder and VLM.</p> <p>The flow-matching branch predicts continuous robot actions using a separate action expert. The VLM prefix is evaluated first, but its KV cache is detached before being consumed by the action expert:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_</span><span class="p">,</span> <span class="n">kv_cache</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">PaliGemma</span><span class="p">.</span><span class="nf">llm</span><span class="p">(</span>
    <span class="p">[</span><span class="n">prefix_tokens</span><span class="p">,</span> <span class="bp">None</span><span class="p">],</span>
    <span class="n">mask</span><span class="o">=</span><span class="n">prefix_attn_mask</span><span class="p">,</span>
    <span class="n">positions</span><span class="o">=</span><span class="n">prefix_positions</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">kv_cache</span> <span class="o">=</span> <span class="n">jax</span><span class="p">.</span><span class="n">tree</span><span class="p">.</span><span class="nf">map</span><span class="p">(</span>
    <span class="n">jax</span><span class="p">.</span><span class="n">lax</span><span class="p">.</span><span class="n">stop_gradient</span><span class="p">,</span>
    <span class="n">kv_cache</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>The resulting gradient topology is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FAST CE
   ├──&gt; SigLIP
   └──&gt; VLM

Flow Matching
   ├──X SigLIP / VLM
   └──&gt; Action Expert + action projections
</code></pre></div></div> <p>Equivalently,</p> \[\nabla_{\theta_{\mathrm{VLM}}}L_{\mathrm{FM}}=0.\] <p>This is the essential role of Knowledge Insulation: the continuous controller can consume VLM features, but the continuous action loss cannot directly rewrite the VLM.</p> <p>The implementation also avoids an unnecessary second image-encoder pass. Images are encoded once by SigLIP, and the resulting image tokens are reused by both branches:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    ┌── FAST VLM forward ──&gt; FAST CE
Images ──&gt; SigLIP ──┤
                    └── FM VLM forward ──&gt; detached KV
                                             │
                                             ▼
                                       Action Expert
                                             │
                                             ▼
                                         Flow loss
</code></pre></div></div> <p>The image representation is shared, but the two VLM forwards remain separate.</p> <p>That distinction matters.</p> <h2 id="2-why-not-also-share-the-vlm-forward">2. Why not also share the VLM forward?</h2> <p>At first, sharing the VLM prefix seems like a natural optimization.</p> <p>Both branches process the same image, language instruction, and robot state. If the VLM could be executed once and its KV cache reused, KI training would require substantially less computation.</p> <p>The difficulty is that, in the legacy FAST formulation used in this implementation, FAST and flow matching do not use the same autoregressive sequence.</p> <p>The FAST tokenizer constructs approximately:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task: &lt;lowercase instruction&gt;, State: &lt;discretized state&gt;;
Action: &lt;FAST action tokens&gt; | &lt;EOS&gt;
</code></pre></div></div> <p>with:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task + State       : bidirectional prefix
Action: + FAST ... : autoregressive postfix
</code></pre></div></div> <p>The CE loss is applied to the postfix, including <code class="language-plaintext highlighter-rouge">Action:</code>.</p> <p>The FAST objective can therefore be written conceptually as:</p> \[p(\mathrm{Action:}, z_1,\ldots,z_N \mid I,\mathrm{Task},\mathrm{State}).\] <p>The standard π0.5 flow-matching prefix is different:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task: &lt;instruction&gt;, State: &lt;state&gt;;
Action:
</code></pre></div></div> <p>Here, <code class="language-plaintext highlighter-rouge">Action:</code> is already part of the conditioning context.</p> <p>The action expert therefore receives features corresponding approximately to:</p> \[p(a \mid I,\mathrm{Task},\mathrm{State},\mathrm{Action:}).\] <p>The two branches therefore do not have identical sequence boundaries.</p> <h2 id="3-what-changes-when-the-vlm-prefix-is-shared">3. What changes when the VLM prefix is shared?</h2> <p>An earlier implementation attempted to share VLM computation by constructing one canonical π0.5 prefix:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image + Task + State + Action:
</code></pre></div></div> <p>and using it for both FAST and flow matching.</p> <p>FAST then predicted only:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FAST_1 FAST_2 ... FAST_N | EOS
</code></pre></div></div> <p>This changes the FAST factorization from:</p> \[p(\mathrm{Action:}, z_{1:N} \mid C)\] <p>to:</p> \[p(z_{1:N} \mid C,\mathrm{Action:}).\] <p>where the context is:</p> \[C=(I,\mathrm{Task},\mathrm{State}).\] <p>This is not merely a computational refactor. It changes what is treated as conditioning information and what is treated as a prediction target.</p> <p>There is also a smaller serialization difference: the legacy FAST tokenizer lowercases the natural-language instruction, whereas the standard π0.5 tokenizer preserves its case.</p> <p>The important point is not that one serialization is universally correct. It is that <strong>sharing the VLM forward is only semantics-preserving when both objectives are defined using the same prefix and autoregressive boundaries</strong>.</p> <h2 id="4-why-the-sequence-difference-may-matter">4. Why the sequence difference may matter</h2> <p>π0.5 pretraining is not simply continuous-action regression. Its VLM is trained using multiple types of autoregressive supervision, including robot-related and semantic outputs.</p> <p>In such a multi-task autoregressive model, tokens such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Action:
</code></pre></div></div> <p>can naturally act as output-type markers:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>context → output type → output contents
</code></pre></div></div> <p>Conceptually, the model might learn structures such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task / State → Action:  → action tokens
Task / State → Subtask: → language tokens
Task / State → object-related outputs
</code></pre></div></div> <p>Under such a formulation, moving <code class="language-plaintext highlighter-rouge">Action:</code> from the predicted suffix into the conditioning prefix changes the autoregressive task presented during post-training.</p> <p>However, this argument is only a <strong>plausibility argument</strong>.</p> <p>The exact internal serialization used during π0.5 pretraining is not publicly available, and the released checkpoint alone cannot uniquely reveal the original training sequence.</p> <p>Therefore, the strongest theoretical statement is:</p> <blockquote> <p>If FAST and flow matching require different autoregressive sequence semantics, forcing them to share the same VLM prefix changes at least one of the objectives.</p> </blockquote> <p>This statement does not require knowing the exact π0.5 pretraining format.</p> <h2 id="5-frozen-checkpoint-likelihood-probe">5. Frozen-checkpoint likelihood probe</h2> <p>I also compared the two serialization choices using the frozen released π0.5-base checkpoint.</p> <p>For every example:</p> <ul> <li>the image was identical;</li> <li>the state was identical;</li> <li>the continuous action trajectory was identical;</li> <li>the resulting FAST action tokens were identical;</li> <li>only the context serialization changed.</li> </ul> <p>The two variants were:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Legacy:
Task(lowercase) + State
    → Action: + FAST tokens

Canonical:
Task + State + Action:
    → FAST tokens
</code></pre></div></div> <p>The main metric compared NLL only on the <strong>same FAST action-code targets</strong>, excluding the <code class="language-plaintext highlighter-rouge">Action:</code> marker itself.</p> <p>On 100 samples from our downstream UR manipulation dataset:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mean action-token CE

Legacy:     16.0106 nats/token
Canonical:  16.1231 nats/token

Canonical - Legacy:
+0.1125 nats/token

Bootstrap 95% CI:
[+0.0430, +0.1870]

Per-sample wins:
Legacy     62 / 100
Canonical  38 / 100
</code></pre></div></div> <p>Pooling all 2,857 matched FAST action tokens gave:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Legacy:     15.7656
Canonical:  15.8369
</code></pre></div></div> <p>The difference is small but statistically consistent on this dataset.</p> <h3 id="what-this-experiment-actually-tells-us">What this experiment actually tells us</h3> <p>This experiment does <strong>not</strong> show that π0.5 was pretrained with the legacy serialization.</p> <p>The evaluation data come from our own downstream UR manipulation dataset rather than the original π0.5 pretraining distribution. The robot embodiment, observations, action distribution, prompts, and task distribution may all differ substantially from those seen during pretraining.</p> <p>Therefore the appropriate interpretation is:</p> <blockquote> <p>On our downstream UR data distribution, the frozen released π0.5-base checkpoint assigns slightly higher likelihood to the same FAST action targets when they are evaluated under the legacy context.</p> </blockquote> <p>This is evidence about <strong>downstream compatibility with the released checkpoint</strong>, not evidence that uniquely identifies the original pretraining recipe.</p> <p>The distinction is important.</p> <p>A stronger claim about pretraining serialization would require similar behavior across multiple datasets and embodiments that better cover the model’s original training distribution. Even then, checkpoint behavior alone would not uniquely recover the original data serialization.</p> <h2 id="6-the-stronger-result-the-optimization-direction-changes">6. The stronger result: the optimization direction changes</h2> <p>The likelihood probe is useful but not the strongest evidence in this study.</p> <p>More importantly, changing the FAST serialization substantially changes the fine-tuning gradient.</p> <p>Several FAST implementations were compared while keeping the model parameters, observations, actions, and random inputs fixed.</p> <p>When comparing the legacy FAST objective against the canonical shared-prefix objective, the gradient cosine similarity was approximately:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image encoder: 0.811
VLM LoRA:      0.855
</code></pre></div></div> <p>The cosine similarity between two gradient vectors is:</p> \[\mathrm{cos}(g_1,g_2) = \frac{g_1^\top g_2}{\lVert g_1\rVert \lVert g_2\rVert}.\] <p>A value near one means that the two implementations produce nearly the same optimization direction.</p> <p>Values around 0.81–0.86 therefore represent a substantial change in the VLM update direction. The VLM gradient norm also increased by approximately 45% under the canonical formulation.</p> <p>In contrast, when the <strong>objective was kept the same</strong> and only the computational path changed from a one-shot forward to an incremental KV-cache forward, the gradient similarities remained around:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image encoder: ~0.990
VLM LoRA:      ~0.993
</code></pre></div></div> <p>There were measurable BF16/XLA numerical differences, but they were much smaller.</p> <p>The comparison therefore looks like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Same objective
one-shot → incremental
        ≈ relatively small numerical change

Legacy FAST objective
→ canonical shared-prefix FAST objective
        ≈ substantially different gradient direction
</code></pre></div></div> <p>This is the most robust conclusion of the experiment:</p> <blockquote> <p>The shared-prefix refactor was not a numerically transparent implementation optimization. It materially changed the FAST optimization objective.</p> </blockquote> <p>This conclusion does not depend on knowing the original π0.5 pretraining serialization.</p> <h2 id="7-interpreting-the-higher-flow-matching-loss-under-ki">7. Interpreting the higher flow-matching loss under KI</h2> <p>Another observation was that the KI run reached a noticeably higher continuous flow-matching loss than conventional LoRA training.</p> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>KI flow loss:
~0.0078

Conventional LoRA flow loss:
~0.0018
</code></pre></div></div> <p>At first glance, this might appear to indicate that KI is not working correctly.</p> <p>That conclusion would be too strong.</p> <p>The two training problems have different optimization constraints.</p> <p>Without KI, the flow-matching loss can update both the action expert and the VLM representation:</p> \[L_{\mathrm{FM}} \rightarrow \{\theta_{\mathrm{VLM}}, \theta_{\mathrm{AE}}\}.\] <p>Under KI, the same loss is constrained to update only the continuous-action path:</p> \[L_{\mathrm{FM}} \rightarrow \theta_{\mathrm{AE}},\] <p>while:</p> \[\nabla_{\theta_{\mathrm{VLM}}} L_{\mathrm{FM}} = 0.\] <p>The KI optimization problem therefore has fewer degrees of freedom for reducing the continuous-action training loss.</p> <p>A higher flow-matching loss is consequently possible even when KI is implemented correctly.</p> <h3 id="a-moving-representation-problem">A moving representation problem</h3> <p>There is an additional asymmetry.</p> <p>FAST continues to update the VLM:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FAST CE
   ↓
VLM representation changes
</code></pre></div></div> <p>while the action expert consumes that representation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>VLM representation
        ↓
   Action Expert
        ↓
     FM loss
</code></pre></div></div> <p>but the FM objective cannot modify the VLM:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FM loss ──X──&gt; VLM
</code></pre></div></div> <p>The action expert is therefore learning on top of a representation that may continue to move during training.</p> <p>This creates a possible <strong>representation-drift / moving-target problem</strong>:</p> \[h_t=f_{\theta_t}(x),\] <p>while:</p> \[\theta_{t+1} = \theta_t - \eta_{\mathrm{VLM}} \nabla L_{\mathrm{FAST}}.\] <p>The action expert must continuously adapt to the changing representation:</p> \[a_t = g_{\phi_t}(h_t).\] <p>This does not imply that KI is flawed. It means that optimization hyperparameters that work for ordinary joint LoRA fine-tuning may not automatically be optimal after the gradient paths are separated.</p> <h2 id="8-ki-may-require-different-optimization-hyperparameters">8. KI may require different optimization hyperparameters</h2> <p>In the current implementation, the VLM LoRA parameters and action-expert parameters are trained under the same optimizer schedule.</p> <p>There is no theoretical reason that the optimal learning rate should be identical for both parameter groups.</p> <p>The VLM is already pretrained and only needs controlled adaptation, while the action expert must fit the continuous downstream control problem.</p> <p>A reasonable KI optimization regime may therefore satisfy:</p> \[\eta_{\mathrm{VLM}} &lt; \eta_{\mathrm{AE}}.\] <p>This would allow the VLM representation to move more conservatively while allowing the action expert to adapt more quickly.</p> <p>For example, a controlled experiment could compare:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>VLM LR : Action Expert LR

1.00 : 1
0.50 : 1
0.25 : 1
0.10 : 1
</code></pre></div></div> <p>while keeping dataset, initialization, batch size, FAST formulation, and training duration fixed.</p> <p>Useful diagnostics would include:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FAST CE
Flow-matching loss
VLM gradient norm
Action-expert gradient norm
xyz / z prediction error
rotation error
gripper prediction error
real-robot success
</code></pre></div></div> <p>If reducing the VLM learning rate lowers the FM loss while preserving useful FAST learning, that would support the hypothesis that representation drift contributes to the optimization gap.</p> <p>If the FM loss remains much higher regardless of the relative learning rates, other explanations become more likely, including action-expert capacity, representation quality, training duration, or limitations of using KI on this downstream dataset.</p> <h3 id="why-simply-changing-the-loss-weights-may-not-be-sufficient">Why simply changing the loss weights may not be sufficient</h3> <p>The KI objective contains explicit weights:</p> \[L = \lambda_{\mathrm{FAST}}L_{\mathrm{FAST}} + \lambda_{\mathrm{FM}}L_{\mathrm{FM}}.\] <p>However, because the two losses largely update disjoint parameter groups, increasing one loss weight is not necessarily equivalent to giving that branch a proportionally larger effective optimization step.</p> <p>With Adam-like optimizers, multiplying a gradient by a constant affects both its first- and second-moment estimates, so much of the scale change can cancel in the normalized update.</p> <p>Therefore, if the goal is specifically to make the VLM adapt more slowly than the action expert, <strong>separate parameter-group learning rates are a more direct control mechanism</strong> than simply changing the two loss weights.</p> <h2 id="9-the-current-implementation-choice">9. The current implementation choice</h2> <p>Given the evidence above, the implementation I currently prefer keeps the computation that can be safely shared while preserving the two autoregressive formulations:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                           ┌─ Legacy FAST VLM
                           │  Task(lowercase) + State
Images ──&gt; shared SigLIP ──┤       ↓
                           │  Action: + FAST tokens
                           │       ↓
                           │     FAST CE
                           │
                           └─ Canonical π0.5 VLM
                              Task + State + Action:
                                      ↓
                                  KV cache
                                      ↓
                                stop_gradient
                                      ↓
                                Action Expert
                                      ↓
                                  Flow loss
</code></pre></div></div> <p>The VLM parameters remain shared.</p> <p>The SigLIP image representation is computed once and reused.</p> <p>The VLM activations and KV caches are computed separately because the two branches currently use different sequence semantics.</p> <p>The distinction is:</p> <blockquote> <p><strong>Parameter sharing is part of KI. Forward-pass sharing is an additional implementation assumption.</strong></p> </blockquote> <p>The latter is safe only when it preserves the objective.</p> <h2 id="10-limitations">10. Limitations</h2> <p>Several limitations are important when interpreting these experiments.</p> <p>First, the frozen-checkpoint NLL comparison was performed on our downstream UR manipulation dataset rather than data sampled from the original π0.5 pretraining distribution.</p> <p>Therefore it cannot establish which serialization Physical Intelligence used during pretraining.</p> <p>The appropriate conclusion is only that the legacy serialization was slightly more compatible with the released checkpoint <strong>on this downstream dataset</strong>.</p> <p>Second, the likelihood difference was small and not universal. The canonical formulation produced lower per-example loss on 38 of 100 samples.</p> <p>Third, the experiments were performed on one robot embodiment and a limited set of manipulation tasks. The result may not generalize to other embodiments, prompts, action spaces, or datasets.</p> <p>Fourth, a higher KI flow-matching loss does not by itself indicate an implementation error. KI changes the optimization problem by preventing FM gradients from updating the VLM. The current KI run also reused hyperparameters designed for a different optimization topology. The effect of parameter-group learning rates and other KI-specific optimization choices has not yet been systematically studied.</p> <p>Fifth, the downstream implementation is not equivalent to Physical Intelligence’s full training recipe. The VLM is mainly adapted using FAST supervision from the same downstream robot dataset, whereas the original KI recipe involves a substantially broader training mixture. This difference may affect representation stability and optimization dynamics.</p> <p>Sixth, real-robot behavior cannot isolate the effect of FAST serialization. Checkpoint selection, data coverage, LoRA configuration, action-expert capacity, optimization hyperparameters, and deployment details can all affect policy performance.</p> <p>Finally, the conclusion is <strong>not</strong> that a shared VLM forward is impossible.</p> <p>A shared forward would be perfectly reasonable if the FAST and continuous-action objectives were designed around the same conditioning sequence.</p> <p>The narrower result is:</p> <blockquote> <p>In this OpenPI KI implementation, sharing the VLM prefix required changing the legacy FAST autoregressive sequence. That change materially altered the VLM gradient direction. Therefore the shared-prefix implementation should be treated as a different training objective rather than a purely computational optimization.</p> </blockquote> <h2 id="takeaway">Takeaway</h2> <p>Knowledge Insulation is primarily about <strong>gradient routing</strong>.</p> <p>It does not require the FAST and continuous-action branches to share identical intermediate activations.</p> <p>For the OpenPI implementation studied here, the conservative design is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shared SigLIP computation
+
shared VLM parameters
+
separate FAST and FM VLM forwards
+
stop-gradient from FM into the VLM
</code></pre></div></div> <p>The current experiments provide strong evidence that replacing the two VLM forwards with one shared canonical prefix changes the FAST optimization objective.</p> <p>They provide only weak, downstream-specific evidence about which serialization is more compatible with the released π0.5-base checkpoint, and they do not reveal the original π0.5 pretraining serialization.</p> <p>Finally, the higher flow-matching loss observed under KI should not immediately be interpreted as a failure of KI. Since KI changes both the gradient topology and the optimization constraints, it likely deserves its own hyperparameter study—especially the relative learning rates of the VLM and action expert.</p>]]></content><author><name></name></author><category term="tech"/><summary type="html"><![CDATA[How Knowledge Insulation separates FAST and flow-matching gradients in OpenPI, and why sharing the VLM forward changes the training objective.]]></summary></entry></feed>